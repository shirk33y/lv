{
  "name": "lv",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "tauri": "tauri",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test && npm run build && cd src-tauri && cargo fmt -- --check && cargo clippy -- -D warnings && cargo test",
    "appimage": "tauri build --bundles appimage",
    "appimage:arm64": "tauri build --target aarch64-unknown-linux-gnu --bundles appimage",
    "win": "npm run build && cd src-tauri && cargo win",
    "dev:win": "cd src-tauri && cargo xwin build --target x86_64-pc-windows-msvc --no-default-features && ./target/x86_64-pc-windows-msvc/debug/lv.exe"
  },
  "dependencies": {
    "@preact/signals": "^2.7.0",
    "@tauri-apps/api": "^2",
    "preact": "^10.25.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.4",
    "@tauri-apps/cli": "^2",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/preact": "^3.2.4",
    "jsdom": "^28.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.0.18"
  }
}
